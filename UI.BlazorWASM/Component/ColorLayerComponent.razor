<div class="layer grid-9x9 color-layer">
    @for( int y = 0; y < 9; y++ )
    {
        for( int x = 0; x < 9; x++ )
        {
            <div class="@ColorClasses[x, y]">

            </div>
        }
    }
</div>

@code {
    [Parameter]
    public string[,] ColorClasses { get; set; }
}

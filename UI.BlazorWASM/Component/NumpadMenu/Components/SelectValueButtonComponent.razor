@using Weboku.UserInterface.Providers
@using Weboku.UserInterface.Component.NumpadMenu.NumpadMenuOptions
@using Weboku.Application
@inject DomainFacade GridProvider
@inject NumpadMenuProvider NumpadMenuProvider

<LabelButtonComponent NumpadMenuLabel="NumpadMenuLabel"></LabelButtonComponent>

@code {
    [Parameter]
    public INumpadMenuLabel NumpadMenuLabel { get; set; }

    protected override void OnInitialized()
    {
        GridProvider.OnGridChanged += StateHasChanged;
        NumpadMenuProvider.OnChanged += StateHasChanged;
    }
}

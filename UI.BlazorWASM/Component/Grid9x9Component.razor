<div class="layer grid-9x9">
    @for( int y = 0; y < 9; y++ )
    {
        for( int x = 0; x < 9; x++ )
        {
            <div class="grid-9x9__cell">
                @ChildContent((x, y))
            </div>
        }
    }
</div>

@code {
    [Parameter]
    public RenderFragment<(int x, int y)> ChildContent { get; set; }
}

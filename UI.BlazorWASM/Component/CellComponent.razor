
    <div class="value input input--@GivenClass">
        @(Cell.Input.Value != 0 ? Cell.Input.Value.ToString() : "")
    </div>

@code {
    [Parameter]
    public ICell Cell { get; set; }

    private string GivenClass { get => Cell.IsGiven ? "is-given " : ""; }

    protected override void OnInitialized()
    {
    }

}
